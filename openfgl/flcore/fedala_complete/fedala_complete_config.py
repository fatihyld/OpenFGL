config = {
    "ala_mu": 0.01,        # Proximal regularization coefficient
    "ala_momentum": 0.9,   # Momentum coefficient (gamma)
    "ala_beta": 0.3,       # Temporal smoothing coefficient
    "ala_meta_init": True,         # Enable meta-learning initialization
    "ala_meta_init_epochs": 3,     # Reduced epochs after first round with meta-init
}
